第一章
事件：用户与对象以不同的方式进行交互时，对象要做不同的事情。（事件是指用户与对象之间的交互）
方法：对象要做的事情。(事件是指用户与对象交互时，触发方法）。
数据建模：事件触发方法，方法可以获取和更新对象。

第三章
可以用方括号法来访问对象的属性，但是不能用来访问方法。
可以用方括号法来更新对象的属性，但是不能用来更新方法。
对象的字面量适用的场景：
      1.在程序之间存储/传输数据时
      2.用于全局对像或为页面设置信息的配置对象
用构造函数创建对象适用的场景：
      1.在同一也中有很多对象，他们有着相似的功能
      2.代码中应该不会有复杂对象